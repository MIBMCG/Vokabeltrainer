# Task 11 – unabhängige Nachprüfung der Korrekturrunde 1

Prüfbasis: **41c3404 → 7d65db15bb4e85978a7d3ea8dcea11ae9ad55006**; Korrekturcode **d026a4b**. Die weiteren enthaltenen Commits betreffen Controllerdokumentation. Umfang: R1–R3 und neue Fehler im Korrekturdiff; keine erneute Gesamtprüfung. M1/M2 sind ausdrücklich Task 13 zugeordnet.

## Finding Verdicts

- **R1 – Auswahl aktiviert die nächste Aktion nicht — ADDRESSED.** `src/trainer/ui/backup.js:118` setzt den Downloadzustand unmittelbar bei Auswahl und Abwahl. `src/trainer/ui/sync.js:236` aktualisiert entsprechend die Schaltfläche für Altänderungen. Der Browser prüft die unmittelbare Auswahl-/Abwahlfolge (`tests/browser/trainer.browser.mjs:1340`) und lädt nach ausdrücklicher Epochenauswahl tatsächlich eine Sicherung herunter (`:1418`). Die zuvor nötige zufällige Hintergrunddarstellung entfällt.
- **R2 – Bestätigung zeigt keine prüfbaren Inhaltsunterschiede oder Abhängigkeiten — NOT ADDRESSED, ein Teilfall bleibt offen.** Der gemeinsame Helper stellt jetzt benannte Punkte, konkrete Vorher-/Nachher-Werte, Wortlernstände, ausgewählte Änderungen und getrennte Supportereignisse dar (`src/trainer/ui/preview.js:167`, `:182`, `:216`, `:226`, `:232`). Auch Konflikte des Zielstands erscheinen mit Fassungen (`:135`). **Weiterhin Important:** Bei einer konfliktbehafteten bisherigen Entität liefert `changeSide` unverändert nur „mehrere sichere Fassungen“ und eine leere Detailliste (`:87`). `conflictNodes` zeigt ausschließlich `summary.conflicts`, also Konflikte des Nachher-Stands (`:136`, `:141`). Wird ein eindeutiges Backup über einen aktuellen Wortkonflikt wiederhergestellt, bleibt deshalb jede bisherige Schreibweise unsichtbar. Erwachsene sehen nicht vollständig, welche Inhalte sie ablösen. Dies ist ein zulässiger Restorepfad: `src/trainer/backup/restore.js:27`–`:33` sperrt hier Epochenkonflikte, nicht Inhaltskonflikte. Eine isolierte Reproduktion mit echten Fixture-, Backup- und Previewfunktionen bestätigt zwei `before.heads`, keinen Nachher-Konflikt, den Platzhalter und **keine** der beiden Vorher-Schreibweisen. Die bestehenden neuen Browserfälle prüfen einen eindeutigen Vorher-Stand (`tests/browser/trainer.browser.mjs:1128`) beziehungsweise Konflikte im Nachher-Stand (`:1284`); den umgekehrten Fall decken sie nicht ab. Korrektur: Auch jede konfliktbehaftete Vorher-Seite anhand ihrer `heads` vollständig und ausdrücklich als „Vorher“ darstellen; dafür alle referenzierten Ereignisse aus dem zur Vorschau passenden aktuellen Stand verfügbar halten, auch nach Aktualisierung. Regression: aktueller Konflikt aus zwei unterschiedlichen Schreibweisen, eindeutiges Zielbackup, beide bisherigen Fassungen und die eindeutige Zielschreibweise sichtbar.
- **R3 – Archivierungs- und Zuordnungskonflikte haben identische Auswahltexte — ADDRESSED.** `src/trainer/ui/preview.js:58` zeigt Aktiv-/Archivstatus, Lektionszuordnung, freigegebene Kinder sowie Wortfelder und unterscheidet Lernstandzuordnungen. `src/trainer/ui/sync.js:192` nutzt diesen Helper für die tatsächlichen Auswahlfassungen. Der neue Browserfall erzeugt reale konkurrierende Profil-, Lektions- und Wortfassungen und prüft Aktiv/Archiviert, Unit 1/2 sowie Ada/kein Kind (`tests/browser/trainer.browser.mjs:1434`, `:1475`). Die Entscheidung beruht damit auf sichtbaren fachlichen Unterschieden.

## New Breakage in the Fix Diff

**None.** Keine zusätzlichen Critical-/Important-/Minor-Befunde im Korrekturdiff. Der verbleibende Vorher-Konfliktfall gehört zum ursprünglichen R2 und ist keine Scopeerweiterung. Der neue Helper bleibt reine Darstellung; Server-Whitelist und Assettest wurden passend ergänzt (`scripts/serve.mjs:37`, `tests/serve.test.js:56`).

## Out-of-Scope Observations

**Keine neuen.** M1 (gemeinsame Statusabbildung) und M2 (Browserprüfung ungebundener Authfehler) bleiben wie beauftragt Task 13 zugeordnet (`docs/superpowers/tasks/task-13-brief.md`). Reale Google-/Apple-Geräteabnahme und PWA werden durch diese Nachprüfung nicht geschlossen.

## Prüfungen und Grenzen

- Vorherige R1–R3, unveränderten Taskbrief aus der Erstprüfung, Nachprüfungsvorlage, angehängten Korrekturbericht und den vollständigen Fixdiff in begrenzten Abschnitten geprüft. Die erste Ausgabe kürzte bereits separat gelesene Controller-/Berichtsdokumentation; sämtliche Code- und Teständerungen waren vollständig lesbar. Anschließend nur Zeilenverweise aus den Diffhunks abgeleitet. Keine Gitbefehle, keine breite Suche und keine zusätzlichen Agenten.
- Der Korrekturbericht dokumentiert RED 0/4 sowie die erwarteten Fehler und das ergänzte R3-RED; anschließend GREEN **4/4** fokussierte Browserfälle und **260/260** Node-Tests mit null Fehlern (`docs/reports/2026-09-18-abgleich-sicherung-oberflaeche.md:55`–`:61`). Die abgedeckten Aussagen wurden mit dem Testdiff abgeglichen; keine Suite wiederholt.
- **Gezielte Vertragskontrolle des neuen Helpers:** `src/trainer/learning/progress.js:223`–`:233` bestätigt die verwendeten `correct/wrong/streak/dueDay`-Felder. `src/trainer/model/revisions.js:157`–`:165` bestätigt `value:null` samt vollständigen `heads` bei Inhaltskonflikten. Der beschriebene Restore-Eingang wurde ausschließlich für den benannten R2-Restfall kontrolliert (`src/trainer/backup/restore.js:27`–`:33`).
- **Gezielte R2-Reproduktion:** Ein Node-Aufruf mit echter `createFixture`, `exportBackup`, `previewBackup` und `previewSummaryNodes`, zwei synthetischen konkurrierenden Wortfassungen und minimalem DOM-Adapter lieferte: `{"beforeConflictHeads":2,"afterConflictCount":0,"showsBeforePlaceholder":true,"showsFirstBeforeVersion":false,"showsSecondBeforeVersion":false}`. Keine neue Browser-/Geräteabnahme behauptet; keine Dateien für diese Reproduktion geschrieben.
- Ausschließlich diesen Reviewbericht geschrieben; keine Produkt-, Index- oder HEAD-Änderung. Mobile Sichtprüfungen bleiben der dokumentierte Implementierernachweis und wurden hier nicht wiederholt.

## Verdict

**Fix round: Findings remain open — R2.** R1 und R3 sind behoben; die vollständige Darstellung der bisherigen Konfliktfassungen fehlt weiterhin. Keine neue Critical-/Important-Regression im Fixdiff.
