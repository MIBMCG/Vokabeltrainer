# Unabhängige Prüfung B3: Elternregler

Reviewer GPT-5.6 Sol / high. Basis `552f0222414f48c588ff2bbc627ac10f12fa678e`, Produktcommit `c04ffd917826ffbb677df94ecc95701a89a585e8`. Ersturteil: Änderungen erforderlich; ein wesentlicher und ein geringfügiger Befund, keine kritischen Befunde.

## Bestätigte Anforderungen

Der Originalkopf bleibt trotz Vorschau/Hintergrundänderung eingefroren; Konflikte erhalten den Entwurf (`learning-rules.js:155`). Bewusstes Neuladen verwendet aktuelle Commands (`185`). Standardwerte verändern nur den Entwurf, Entwürfe bleiben je Profil getrennt und bei Bereichswechseln erhalten. Wiederüben prüft aktuelle Zuordnung, Archivstatus, Ausschluss und übergibt Lernfassung/Generation (`vocabulary.js:578`). Die PIN-Bediengrenze bleibt erhalten; Route und Offlinecache sind erweitert. Die geprüfte Mobilansicht ist lesbar.

## I1: veralteter Stopwert-Vorschlag

`src/trainer/ui/learning-rules.js:137` ersetzt den Stopwert beim Ausschalten von Auffrischen nur, wenn das Feld leer ist. Da der Wert beim Öffnen bereits mit 6 gefüllt wird, zeigt ein Entwurf nach Änderung der Schwelle auf 10 weiterhin einen Stopwert 6. Dieser von der Oberfläche vorgeschlagene Zustand ist ungültig und widerspricht dem vereinbarten Vorschlag `Math.max(6, slowAfter)`. Der bewusste Togglewechsel muss den Vorschlag aus der aktuell gewählten Schwelle bilden.

## M1: Standardwerte in der Anleitung

`docs/BENUTZUNG.md:35` nennt drei richtige Antworten und Abstände 1/3/7/14 ohne Kennzeichnung als Standard. Der neue Abschnitt erklärt dagegen die Einstellbarkeit. Die Formulierung ist anzugleichen.

Der Controller hat beide Stellen vor Beauftragung der gezielten Fixrunde geprüft.

## Nachprüfung: freigegeben

Fix `2f0a12615cf80c09920a08d9b8c794deeaecfd67` wurde durch denselben unabhängigen Sol/high-Reviewer begrenzt nachgeprüft. I1 und M1 sind vollständig behoben. Der bewusste Togglewechsel verwendet die aktuelle Schwelle (`learning-rules.js:139`), während eine reine Schwellenänderung individuelle Stopwerte erhält. Die Anleitung kennzeichnet die Standardwerte (`BENUTZUNG.md:35`). Keine neuen wesentlichen Fixfolgen; Cache v17 / synthetisch v18 sind konsistent. Spezifikation PASS, Qualität APPROVED. Keine offenen B3-Reviewbefunde; der getrennte C2-Prüfhinweis bleibt offen.

## Nachweisgrenzen

Keine redundante Ausführung der gemeldeten Tests. Der breite Überarbeitungslauf ist mit 11/12 nicht vollständig grün. Der unveränderte Offline-Avatar-Diagnoseknoten kann durch späteres Rendern entfernt werden; der B3-Diff belegt keine konkrete Produktursache. Die isolierte Wiederholung bestand 1/1, eine stabile Gesamtausführung bleibt aber Teil von C2. Der [Implementierungsbericht](2026-09-19-b3-elternregler.md) erhält diesen Hinweis ausdrücklich. Reale Google-, Zwei-Geräte-, iOS- und Safari-Prüfungen sind weiterhin offen.
