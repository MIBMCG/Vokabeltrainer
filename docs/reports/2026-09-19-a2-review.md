# Unabhängige Prüfung A2: Rundenstart und Übungsansicht

Erstprüfung: GPT-5.6 Sol, Denktiefe high. Basis `b0e826c5b7e78d4aa5fa9727e317bf991c905a2c`, geprüfter Stand `10987b5ffc556fdeda95d8295811595a36b0ba22`.

## Ergebnis der Erstprüfung

Der abgestimmte Umfang ist umgesetzt. `activeWords`, `modeWords` und `previewModes` teilen die Kandidatenlogik; Anzahl, Gründe, Reihenfolge und zukünftiger Scheduling-Vertrag stimmen (`src/trainer/learning/rounds.js:63`, `:79`, `:116`, `:129`). `practiceChoices` verwendet injizierte Uhr und Datensatzzeitzone ohne Mutation (`src/trainer/commands.js:494-498`). Radiokarten, gespeicherte Auswahl und Größe sowie ein einzelner geschützter Submit sind vorhanden (`src/trainer/ui/practice.js:153`, `:218`, `:256`, `:282`).

Die Tests decken archivierte, nicht zugeordnete, neue, nicht fällige und konfliktbehaftete Wörter, die spätere Scheduling-Map sowie doppelte Starts/Antworten ab. Kleine Ansicht, Querformat und 200 Prozent Schrift sind geprüft; die Schrift wird nicht zum Einpassen verkleinert. Keine kritischen oder geringfügigen Befunde.

Ein wesentlicher Befund: Bei einem abgelehnten `commands.start` blieb der Startbutton deaktiviert, weil der Catch nur `ui.busy` zurücksetzte und ohne erneutes Rendern zur Startansicht wechselte (`src/trainer/ui/practice.js:240`, `:245-248`). Ein unmittelbarer Wiederholungsversuch war dadurch blockiert.

## Gezielte Korrektur

Commit `e0c237f1c8ff3a06602deba2030a5b1e06586475` berechnet nach dem Fehler den Zustand des Startbuttons erneut. Ein absichtlich einmalig fehlgeschlagener IndexedDB-Schreibzugriff belegt zuerst den Fehler und anschließend die Korrektur: unveränderte Auswahl `latest` und 20 Antworten, keine Runde nach dem Fehler, genau eine nach erneutem Start einschließlich Doppelauslösung. Der gezielte Retryfall und der kontrollierte Worker-Updatefall bestanden jeweils 1/1. Produktcache `v9`, synthetischer Updateworker `v10`.

Die unabhängige Nachprüfung mit GPT-5.6 Sol / medium bestätigt den Befund als behoben. Im begrenzten Fix-Diff entstanden keine neuen wesentlichen Fehler; keine weiteren Befunde. Die vorhandenen gezielten RED/GREEN-Belege wurden geprüft, ohne die Tests nochmals auszuführen. Der [Implementierungsbericht](2026-09-19-a2-rundenstart.md) enthält die vollständigen Testbelege und tatsächlichen Ansichten.
