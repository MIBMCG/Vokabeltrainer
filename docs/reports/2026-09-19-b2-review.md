# Unabhängige Prüfung B2: Lernplanung

Reviewer GPT-5.6 Sol / high. Basis `d1cf3027834057ed5ce0e23740f3bd1cc6987928`, Produktcommit `68909b55a1f66cf23d2bd3bbfbc10d552cee6c81`. Spezifikation: PASS. Qualität: APPROVED. Keine kritischen, wesentlichen oder geringfügigen Befunde; keine Fixrunde erforderlich.

## Bestätigte Verträge

- Unverändert gemeinsam ausgelagerte Ereignisordnung und Antwortslot-Deduplizierung (`src/trainer/learning/facts.js:13`). Die Belohnungsprojektion verwendet diese Funktionen ohne fachliche Änderung.
- Ganze Policy- und Generationsgewinner ausschließlich aus geordneten effektiven Ereignissen (`src/trainer/model/policies.js:20`). Unterstützende Ereignisse ersetzen keine aktuellen Gewinner.
- Deterministische Schwellen, Intervalle, Fehlerlücke, Stopkriterium und Generationsfilter in der getrennten Projektion (`src/trainer/learning/schedule.js:6`). Alte/unterlegene Generationen zählen weiterhin in Punkten und Versuchen, nicht in der aktuellen Scheduling-Serie.
- Schedulingdaten bestimmen Verfügbarkeit; bestehende Fakten bestimmen weiterhin die Rangfolge (`src/trainer/learning/rounds.js:159`).
- Policy und Kandidatengenerationen bleiben innerhalb der Runde eingefroren. Erweiterungen ergänzen aktuelle Generationen unter derselben gespeicherten Policy (`src/trainer/commands.js:437`). Legacy-Runden behalten ihre bisherige Planung.
- Regeländerung und Wiederaktivierung prüfen erwartete Gewinner innerhalb der serialisierten Mutation (`src/trainer/commands.js:572`). Lokale Rundenvalidierung bindet ihre Regeln und Generationen an Ereignisreferenzen.

Tests behandeln konkurrierende Regeln/Resets, umgekehrte Ankunft, Support/Adoption, verspätete echte v1-Antworten, Reload, Erweiterung, Legacy und unveränderte Belohnungen. Neue Module sind in Serverfreigabe und Offlinecache enthalten.

## Nachweisgrenzen

Die belegten 339/339 Node- und 4/4 Browserprüfungen wurden nicht redundant ausgeführt. Die Review las den begrenzten B2-Diff und den [Implementierungsbericht](2026-09-19-b2-lernplanung.md). Physische Apple-/Zwei-Geräte- und reale Produkt-Drive-Abnahme bleiben außerhalb dieser synthetischen Prüfungen und zutreffend offen.
